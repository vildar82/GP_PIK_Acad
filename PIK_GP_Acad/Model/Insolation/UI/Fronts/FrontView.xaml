<UserControl
    x:Class="PIK_GP_Acad.Insolation.UI.FrontView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:PIK_GP_Acad.Insolation.UI"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    d:DataContext="{d:DesignInstance local:FrontViewModel,
                                     IsDesignTimeCreatable=True}"
    d:DesignHeight="300" d:DesignWidth="300"
    mc:Ignorable="d">
    <UserControl.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="/PIK_GP_Acad;component/Model/Insolation/UI/Resources/InsPaletteStyle.xaml" />
            </ResourceDictionary.MergedDictionaries>
        </ResourceDictionary>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <!--  Список групп - областей расчета фронтов  -->
        <ItemsControl
            x:Name="lbGroups"
            Grid.Row="0"
            Background="Transparent"
            ItemsSource="{Binding Front.Groups, UpdateSourceTrigger=PropertyChanged}">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border Margin="5" BorderBrush="Gray" BorderThickness="1">
                        <Expander Content="{Binding}" Header="{Binding}" IsExpanded="{Binding IsExpanded}">
                            <!--  Заголовок группы - все о группе  -->                            
                            <Expander.HeaderTemplate>
                                <DataTemplate>
                                    <Grid Margin="5"
                                          HorizontalAlignment="{Binding HorizontalAlignment, Mode=OneWayToSource, RelativeSource={RelativeSource FindAncestor, AncestorType={x:Type ContentPresenter}}}">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition />
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Grid.Column="0" Margin="5" Text="{Binding Name}" />
                                        <Button Grid.Column="1" Margin="5" Content="{DynamicResource delete}"/>
                                    </Grid>
                                </DataTemplate>
                            </Expander.HeaderTemplate>
                            <!--  Список домов в группе  -->
                            <Expander.ContentTemplate>
                                <DataTemplate>
                                    <ListBox ItemsSource="{Binding Houses}">
                                        <ListBox.ItemTemplate>
                                            <DataTemplate>
                                                <TextBlock Text="{Binding Name}" />
                                            </DataTemplate>
                                        </ListBox.ItemTemplate>
                                    </ListBox>
                                </DataTemplate>
                            </Expander.ContentTemplate>
                        </Expander>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
        <!--  Кнопки управления  -->
        <Grid Grid.Row="1">
            <Button Content="Добавить группу" />
        </Grid>
    </Grid>
</UserControl>
