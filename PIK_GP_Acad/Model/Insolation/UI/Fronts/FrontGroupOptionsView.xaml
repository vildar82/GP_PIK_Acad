<Window x:Class="PIK_GP_Acad.Insolation.UI.FrontGroupOptionsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:PIK_GP_Acad.Insolation.UI"
        xmlns:xc="clr-namespace:ExCastle.Wpf"
        xc:DialogCloser.DialogResult="{Binding DialogResult}"
        mc:Ignorable="d"
        Title="FrontOptionsView" Height="213" Width="258">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <local:WindowOptionsView Grid.Row="0" DataContext="{Binding WindowVM, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
        <StackPanel
            Grid.Row="1"
            Margin="2" HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button
                Width="60"
                Margin="5" HorizontalAlignment="Center"
                Command="{Binding OK}"
                Content="OK"                 
                IsDefault="True"/>
            <Button
                Width="60"
                Margin="5" HorizontalAlignment="Center"                
                Content="Cancel" 
                IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
