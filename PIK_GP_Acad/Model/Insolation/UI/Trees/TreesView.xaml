<UserControl x:Class="PIK_GP_Acad.Insolation.UI.Trees.TreesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PIK_GP_Acad.Insolation.UI.Trees"                          
             mc:Ignorable="d">    
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--Список расчетных точекк-->
        <DockPanel Grid.Column="0">
            <StackPanel>
                <ListBox ItemsSource="{Binding Points}" Background="Transparent">
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <StackPanel>
                                <TextBlock Text="{Binding Number}" Background="{Binding Color}"/>
                            </StackPanel>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <Button Content="Добавить точку" Command="{Binding AddPoint}"/>
            </StackPanel>
        </DockPanel>
        <!--Настройки высот для визуализации-->
        <StackPanel Grid.Column="1">
            <ListBox ItemsSource="{Binding VisualOptions}" HorizontalContentAlignment="Stretch" Background="Transparent">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <DockPanel>
                            <TextBox Text="{Binding Height, StringFormat=\{0\} м}" HorizontalAlignment="Stretch" BorderThickness="0">
                                <TextBox.Background>
                                    <SolidColorBrush Color="{Binding Color}"/>
                                </TextBox.Background>
                            </TextBox>
                        </DockPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </StackPanel>
    </Grid>
</UserControl>
